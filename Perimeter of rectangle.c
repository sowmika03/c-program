#include<stdio.h>
void main() 
{
    int l, b, perimeter;
    printf("enter the values of l, b:");
    scanf("%d%d",&l,&b);
    perimeter=2*(l+b);
    printf("perimeter of rectangle is:%d",perimeter);
}
