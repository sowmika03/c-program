#include <stdio.h>
int main() 
{
    int n=0,a=10;
    while(n<=10)
    {
      printf("%d",n);
      n++;  
    }
    printf("\n");
    while(a>=0)
    {
      printf("%d",a);
      a--;  
    }

    return 0;
}
